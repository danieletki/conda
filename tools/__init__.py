"""Repository-local developer tooling.

The `tools/` tree is intentionally excluded from the built `conda` distribution (see
`pyproject.toml`). It can host experimental or internal utilities without
impacting the shipped library.
"""
